export enum Logic {
    OR = "OR",
    AND = "AND"
}

export enum MComparator {
    LT = "LT",
    GT = "GT",
    EQ = "EQ"
}

export enum SComparator {
    IS = "IS"
}

export enum Negation {
    NOT = "NOT"
}

export enum MFieldCourse {
    avg = "avg",
    pass = "pass",
    fail = "fail",
    audit = "audit",
    year = "year"
}

export enum SFieldCourse {
    dept = "dept",
    id = "id",
    instructor = "instructor",
    title = "title",
    uuid = "uuid"
}

export enum MFieldRoom {
    lat = "lat",
    lon = "lon",
    seats = "seats"
}

export enum SFieldRoom {
    fullname = "fullname",
    shortname = "shortname",
    number = "number",
    name = "name",
    address = "address",
    type = "type",
    furniture = "furniture",
    href = "href"
}

export enum ApplyToken {
    MAX = "MAX",
    MIN = "MIN",
    AVG = "AVG",
    COUNT = "COUNT",
    SUM = "SUM"
}
